# --- Machine learning / data core: installed and exercised end-to-end
# (data pipeline, training, tuning, SHAP/LIME, tests) in this environment.
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.3.0
lightgbm==4.7.0
shap==0.52.0
lime==0.2.0.1
joblib==1.5.3
python-dotenv==1.2.2
pytest==9.1.1

# --- Planned service/database layer (README roadmap items 5-7: database,
# FastAPI, dashboard). Not yet installed or exercised in this environment
# -- src/api, src/database, and frontend/ are still empty. The previous
# pins here (psycopg[binary]==3.12.0, alembic==1.34.0) were not real
# published releases and would have made `pip install -r requirements.txt`
# fail outright; corrected to real current releases as a placeholder, but
# treat as unverified until that layer is actually built.
fastapi==0.111.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.3
alembic==1.13.2
psycopg[binary]==3.2.3
pydantic==2.8.3
httpx==0.27.0
plotly==5.18.0
